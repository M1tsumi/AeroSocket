[package]
name = "aerosocket"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
documentation.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
description.workspace = true

[features]
default = ["tokio-runtime", "transport-tcp"]

# Runtime features
tokio-runtime = ["tokio", "aerosocket-core/tokio-runtime"]
async-std-runtime = ["async-std", "aerosocket-core/async-std-runtime"]

# Transport features
transport-tcp = ["aerosocket-transport-tcp"]
transport-tls = ["aerosocket-transport-tls"]

# Platform features
server = ["aerosocket-server"]
client = ["aerosocket-client"]
# wasm = ["aerosocket-wasm"]  # Temporarily disabled

# Serialization features
serde = ["aerosocket-core/serde"]
rkyv = ["aerosocket-core/rkyv"]

# Full feature set
full = [
    "tokio-runtime",
    "transport-tcp", 
    "transport-tls",
    "serde",
    "rkyv"
]

[dependencies]
aerosocket-core = { path = "../aerosocket-core", version = "0.1.0" }
aerosocket-server = { path = "../aerosocket-server", version = "0.1.0", optional = true }
aerosocket-client = { path = "../aerosocket-client", version = "0.1.0", optional = true }
aerosocket-transport-tcp = { path = "../aerosocket-transport-tcp", version = "0.1.0", optional = true }
aerosocket-transport-tls = { path = "../aerosocket-transport-tls", version = "0.1.0", optional = true }
# aerosocket-wasm = { path = "../aerosocket-wasm", version = "0.1.0", optional = true }  # Temporarily disabled

# Optional runtime dependencies
tokio = { workspace = true, optional = true }
async-std = { version = "1.12", optional = true }

[dev-dependencies]
tokio = { workspace = true }
tokio-test = { workspace = true }
tracing-test = { workspace = true }

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
